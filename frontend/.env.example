# Copy to .env.local (which is gitignored) and adjust as needed.

# Base URL for the LedgerFlow backend (FastAPI).
# Default is local dev; for production, point at your deployed backend.
VITE_API_URL=http://localhost:8000
